# Frontend-specific variables (must have VITE_ prefix)
VITE_API_URL=/health-tracker/api
VITE_APP_TITLE=health-tracker
VITE_DEBUG_MODE=true
VITE_TEST="health-tracker_test"

# Port configurations. 
# Please do not change the ports manually, these are bind to the application and cannot be changed at runtime.
# You might face re-direction issue when application starts.
FRONTEND_PORT=16972
BACKEND_PORT=16973

# Backend-specific variables
NODE_ENV=development
LOG_LEVEL=debug
TEST="health-tracker_test"

# CORS settings
CORS_ORIGIN=http://localhost:${FRONTEND_PORT}

#MongoDB database name
DB_NAME=health_tracker_db

#UserSpace path
USERSPACE_BASE_PATH=/ext-data/code-server

VITE_ORCHESTRATOR_SERVICE_BASE_URL=https://stage.piramalfinance.com/api/byot/orchestrator
ORCHESTRATOR_SERVICE_BASE_URL=https://stage.piramalfinance.com/api/byot/orchestrator
VITE_AUTH_LOGIN_API_ENDPOINT=/login/sso
VITE_AUTH_LOGOUT_API_ENDPOINT=/logout